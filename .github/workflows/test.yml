      - name: Compile and Run C Tests
        if: env.RUN_C == '1'
        run: |
          set +e
          gcc -o tests/test tests/test.c solutions/solution.c
          ./tests/test > /dev/null 2>&1
          STATUS=$?
          if [ $STATUS -ne 0 ]; then
            echo "❌ Code is wrong"
            exit 1
          fi
          echo "✅ Code passed all tests"

      - name: Compile and Run C++ Tests
        if: env.RUN_CPP == '1'
        run: |
          set +e
          g++ -o tests/test tests/test.cpp solutions/solution.cpp
          ./tests/test > /dev/null 2>&1
          STATUS=$?
          if [ $STATUS -ne 0 ]; then
            echo "❌ Code is wrong"
            exit 1
          fi
          echo "✅ Code passed all tests"

      - name: Run Python Tests
        if: env.RUN_PYTHON == '1'
        run: |
          set +e
          export PYTHONUNBUFFERED=1
          export PYTHONPATH=$PWD
          python3 tests/test.py > /dev/null 2>&1
          STATUS=$?
          if [ $STATUS -ne 0 ]; then
            echo "❌ Code is wrong"
            exit 1
          fi
          echo "✅ Code passed all tests"

      - name: Compile and Run Java Tests
        if: env.RUN_JAVA == '1'
        run: |
          set +e
          mkdir -p out
          javac -d out solutions/Solution.java tests/Test.java
          java -cp out Test > /dev/null 2>&1
          STATUS=$?
          if [ $STATUS -ne 0 ]; then
            echo "❌ Code is wrong"
            exit 1
          fi
          echo "✅ Code passed all tests"
