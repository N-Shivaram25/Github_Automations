#include <iostream>
#include <vector>
using namespace std;

// Use the same struct definition as in test.cpp s
struct ListNode {
    int val;
    ListNode* next;
    ListNode(int x) : val(x), next(nullptr) {}
};

// Implement the main solution functionsssa
ListNode* removeElements(ListNode* head, int val) {
    
}